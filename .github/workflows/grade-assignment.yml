name: Grade Assignment

on:
  pull_request_target:
    branches:
      - main

jobs:
  grade:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      issues: write
      pull-requests: write

    steps:
      - name: Checkout student code
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          path: assignment

      # Overlay .hyf/ from the base repo so grading scripts are always current,
      # regardless of when the student forked.
      - name: Checkout base repo grading scripts
        uses: actions/checkout@v4
        with:
          path: base-repo

      - name: Overlay .hyf/ from base repo
        run: cp -r base-repo/.hyf/. assignment/.hyf/

      - name: Checkout workflow scripts
        uses: actions/checkout@v4
        with:
          repository: HackYourFuture/github-actions
          path: workflow-scripts

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "lts/*"

      - name: Run tests
        working-directory: assignment/.hyf
        run: bash test.sh > test-output.txt 2>&1

      - name: Comment PR
        uses: actions/github-script@v7
        with:
          script: |
            const path = require('path');
            const run = require(path.join(process.env.GITHUB_WORKSPACE, 'workflow-scripts/workflow-scripts/comment-pr.js'));
            process.chdir(path.join(process.env.GITHUB_WORKSPACE, 'assignment'));
            await run({ github, context, core });

      - name: Fail if tests failed
        run: |
          PASS=$(jq -r '.pass' assignment/.hyf/score.json)
          if [ "$PASS" != "true" ]; then
            exit 1
          fi
